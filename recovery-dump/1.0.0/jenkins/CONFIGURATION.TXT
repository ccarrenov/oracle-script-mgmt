CREATE JOBS WITH EXECUTE NEXT SCRIPT

#!/bin/bash +x
python recovery-dump.py "${HOST_NAME}" "${HOST_PORT}" "${USER_NAME}" "${SERVER_PASS}" "${EVN_ORACLE_SID}" "${ORACLE_USER}" "${ORACLE_PORT}" "${ORACLE_PASS}" "${ORACLE_SCHEME_ORIGIN}" "${ORACLE_SCHEME_SEND}" "${TABLE_SPACE_DATA_ORIGIN}" "${TABLE_SPACE_DATA_SEND}" "${TABLE_SPACE_TMP_ORIGIN}" "${TABLE_SPACE_TMP_SEND}" "${DIRECTORY}" "${DUMP_FILE}" "${LOG_FILE}" "${ORACLE_HOME}"
